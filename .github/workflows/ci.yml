# Official CI — triggers only; composable checks live in ci-checks.yml.
# Branch protection: require job ci.

name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    uses: $/.github/workflows/ci-checks.yml
